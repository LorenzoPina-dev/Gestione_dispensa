# OpenAPI contracts

The canonical public HTTP contract is currently documented in `docs/openapi.yaml`. This folder
is reserved for split, generated, and versioned API specifications once the API surface is
materialized by service boundary.
