# Search indexer

Rebuildable projection consumer for catalog, inventory, and shopping search. PostgreSQL remains
authoritative; an optional OpenSearch adapter can be introduced after measured benchmarks.
